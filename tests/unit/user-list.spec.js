import {shallowMount} from '@vue/test-utils'
import UserList from '@/components/UserList.vue'

describe('UserList', () => {

    it('Renders a list of UserItems', () => {
        expect(true).toBe(true);
    });
});
