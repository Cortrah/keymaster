import {shallowMount} from '@vue/test-utils'
import UserItem from '@/components/UserItem.vue'

describe('UserItem', () => {

    it('Renders a UserItem and allows one to archive it or add Permissions to it', () => {
        expect(true).toBe(true);
    });
});
